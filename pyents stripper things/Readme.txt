Open the command prompt in the directory (type cmd in the directory path)
Put both bsp in the directory

Command:

To get the entity list, you can use this:
dump.py original.bsp > entitydump.txt

To get the stripper, use this:
entities.py original.bsp modified.bsp > output.txt